@model Quote
<div class="container mt-4">
    <h2>Quote Details</h2>
    <blockquote>@Model.Text</blockquote>
    <footer>@Model.Author (@Model.Category)</footer>
    <p>Likes: @Model.Likes | Created: @Model.CreatedAt</p>
    <a class="btn btn-secondary" asp-action="Index">Back</a>
</div>

